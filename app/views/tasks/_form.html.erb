<%= form_for([@task.project, @task]) do |f| %>
<%#= form_for([@task.project, @task], html: { class: 'form-inline'}) do |f| %>

<b><%= f.label :name %></b>
<p><%= f.text_field :name %></p>

<b><%= f.label :description %></b>
<p><%= f.text_area :description %></p>

<b><%= f.label :status %></b>
<p><%= f.select :status, Task::STATUS_OPTIONS, {} %></p>

<%= f.submit %>

<% end %>